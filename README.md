# Prime-Number-Checker-PYTHON-
This project is a simple Prime Number Checker built using Python. The program takes a number as input and checks whether it is a prime number or not using basic loop and conditional logic. It also includes input validation and allows the user to test multiple numbers until exit.
